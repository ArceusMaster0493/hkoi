#include<stdio.h>
#include<string.h>
int main(){
    char in[256];
    int cl=0,cw,i;
    scanf("%[^\n]",&in);
    cw=strlen(in);
    for(i=0;i<cw;i++){
        if(in[i]==' '){
            cl=cl+1;
        }
    }
    printf("%d\n",cw);
    printf("%d\n",cl+1);
    return 0;
}
